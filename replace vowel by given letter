#Write a function, that replaces all vowels in a string with a specified vowel.

def vow_replace(str,a):
    result=""
    for char in str:
        if char in 'aeiouAEIOU':
            result+=a
        else:
            result+=char
    return result
str=input()
a=input()
print(vow_replace(str,a))
